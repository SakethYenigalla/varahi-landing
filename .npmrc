; Project-local npm config — overrides the user's global ~/.npmrc
; for this folder only. Keeps the personal Varahi project on the public
; registry while leaving the user's default (work registry) alone.
registry=https://registry.npmjs.org/
